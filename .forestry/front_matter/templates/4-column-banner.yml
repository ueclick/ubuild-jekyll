---
label: 4-column-banner
hide_body: true
fields:
- name: background_image
  type: file
  config:
    maxSize: 250
  label: background image
- name: column1
  type: field_group
  config: {}
  fields:
  - name: image
    type: file
    config:
      maxSize: 250
    label: image
  - name: title
    type: text
    config:
      required: false
    label: title
  - name: content
    type: list
    config:
      use_select: false
      min: 
      max: 
    label: content
  label: column1
