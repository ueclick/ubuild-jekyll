---
label: list-column
hide_body: true
display_field: title
fields:
- name: title
  type: text
  config:
    required: false
  label: title
- name: content
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: content
- name: image
  type: file
  config:
    maxSize: 250
  label: image
